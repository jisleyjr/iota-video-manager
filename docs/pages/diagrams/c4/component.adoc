= Components

NOTE: This document describes the planned product architecture. The implemented API currently provides only the Spring Boot/MySQL foundation and health endpoints.

== Application API

The API includes stored YouTube video queries, a scheduled synchronization task, and a server-side YouTube API client. See xref:api/youtube-monitoring.adoc[YouTube monitoring API] for configuration, synchronization, and read endpoints. GPU processing remains a separate workflow.

[structurizr]
....
workspace "Iota Video Manager" "Application API component view" {

include::{docdir}/../../../partials/diagrams/c4/model.dsl[]

    views {
        component applicationApi "application-api-components" {
            include *
            autoLayout lr
        }
include::{docdir}/../../../partials/diagrams/c4/styles.dsl[]
    }
}
....
